language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-simloovoo' ]; then cd .. && eval "mv $currentfolder generator-simloovoo" && cd generator-simloovoo; fi

